#include <iostream>
#include <string>
using namespace std;

int op_ca(string s)
{
    for(int i = 0; i < s.size(); i++)
    {
        for(int j = 0; j < s.size(); j++)
        {
            
        }
    }
}

int main()
{
    string s;
    getline(cin, s);
    int ans = op_ca(s);
    cout<<ans<<endl;
}